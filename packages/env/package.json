{
  "name": "env",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsup",
    "dev": "dotenv -e .env -- tsup --watch"
  },
  "dependencies": {
    "dotenv-cli": "^7.2.1"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*"
  }
}